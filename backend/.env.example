HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Database — Supabase Postgres via pgbouncer pooler (port 6543)
# Get these values from: Supabase Dashboard → Project Settings → Database → Connection Pooling
DATABASE_CLIENT=postgres
DATABASE_HOST=
DATABASE_PORT=6543
DATABASE_NAME=postgres
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=
# SSL is always enforced in code (rejectUnauthorized: false) — no env var needed for SSL.
